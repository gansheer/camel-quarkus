# Client port of 4222 on all interfaces
port: 4222

tls {
  # TLS handshake timeout in fractional seconds. Default set to 0.5 seconds.
  # This value has been raised to 3s to avoid TLS handshake errors on busy build servers
  timeout: 3
}
